# image-backend/templates/service.yaml 파일 
apiVersion: v1
kind: Service
metadata:
  name: svc-image-backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
  selector:
    app: image-backend